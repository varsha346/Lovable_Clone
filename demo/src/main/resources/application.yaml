spring:
  application:
    name: lovable-clone
    description: this is a app

  datasource:
    url: jdbc:postgresql://localhost:5432/lovable   # usually 5432, see note below
    username: postgres                              # usually 'postgres'
    password: v1a2r3s4h5a6
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
